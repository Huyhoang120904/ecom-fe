import Link from "next/link";
import { ProductCard } from "@/components/common/ProductCard";
import { Button } from "@/components/ui";
import { categories, products } from "@/lib/mock-data";

export default function ProductsPage() {
  return <><section className="page-hero"><div className="container"><div className="breadcrumbs"><Link href="/">Trang chủ</Link><span>›</span><span>Tất cả sản phẩm</span></div><h1>Tất cả sản phẩm</h1><p className="section-description">Khám phá những lựa chọn được tuyển chọn từ các shop độc lập trên ecome.</p></div></section><div className="container listing-layout"><aside className="filter-panel"><h3>Bộ lọc</h3><div className="filter-group"><strong>Danh mục</strong>{categories.slice(0, 5).map((category) => <label key={category.id} className="check-row"><input type="checkbox" /> {category.name}<span className="muted">({category.count})</span></label>)}</div><div className="filter-group"><strong>Khoảng giá</strong><div className="price-inputs"><input className="field-input" placeholder="Từ" /><input className="field-input" placeholder="Đến" /></div></div><div className="filter-group"><strong>Đánh giá</strong>{[5, 4, 3].map((star) => <label key={star} className="check-row"><input type="checkbox" /> <span className="stars">{"★".repeat(star)}</span> trở lên</label>)}</div><div className="filter-group"><strong>Thương hiệu</strong>{["Aurora", "Lumi Living", "Mộc Studio"].map((brand) => <label key={brand} className="check-row"><input type="checkbox" /> {brand}</label>)}</div><Button variant="outline" className="button-sm" style={{ width: "100%" }}>Xóa bộ lọc</Button></aside><section><div className="listing-toolbar"><p>Hiển thị <strong>{products.length}</strong> sản phẩm</p><select className="sort-select" defaultValue="featured" aria-label="Sắp xếp sản phẩm"><option value="featured">Phổ biến nhất</option><option value="new">Mới nhất</option><option value="price-low">Giá thấp đến cao</option><option value="price-high">Giá cao đến thấp</option></select></div><div className="product-grid">{products.map((product) => <ProductCard key={product.id} product={product} />)}</div><div className="pagination"><span className="page-number active">1</span><span className="page-number">2</span><span className="page-number">3</span><span className="page-number">→</span></div></section></div></>;
}

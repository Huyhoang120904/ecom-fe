import { Button, RatingStars } from "@/components/ui";
import { products, reviews } from "@/lib/mock-data";

export default function SellerReviewsPage() { return <><div className="seller-titlebar"><div><p className="eyebrow">Phát triển</p><h1>Đánh giá</h1><p>Phản hồi để xây dựng mối quan hệ lâu dài với khách hàng.</p></div><Button variant="outline">Xuất đánh giá</Button></div><div className="seller-grid"><section className="seller-card"><div style={{ display: "flex", gap: 20, alignItems: "center" }}><div><strong style={{ display: "block", fontSize: 38 }}>4.9</strong><RatingStars rating={4.9} /><span className="muted" style={{ display: "block", marginTop: 5, fontSize: 11 }}>Từ 1,284 đánh giá</span></div><div style={{ flex: 1 }}>{[5, 4, 3, 2, 1].map((star, index) => <div key={star} style={{ display: "flex", gap: 9, alignItems: "center", margin: "5px 0" }}><span style={{ width: 35, color: "var(--muted)", fontSize: 10 }}>{star} sao</span><div className="progress" style={{ flex: 1 }}><span style={{ width: `${[88, 8, 3, 1, 0][index]}%` }} /></div><span className="muted" style={{ width: 25, fontSize: 10 }}>{[88, 8, 3, 1, 0][index]}%</span></div>)}</div></div></section><section className="seller-card"><h2>Phản hồi cần xử lý</h2><p className="muted" style={{ fontSize: 11 }}>2 đánh giá mới trong hôm nay</p><Button style={{ marginTop: 15 }}>Xem đánh giá mới</Button></section></div><section className="seller-card" style={{ marginTop: 16 }}><div className="seller-card-heading"><h2>Tất cả đánh giá</h2><select className="sort-select" defaultValue="all" aria-label="Lọc đánh giá"><option value="all">Tất cả đánh giá</option><option>Chưa phản hồi</option><option>5 sao</option></select></div>{reviews.map((review, index) => <div className="review-card" key={review.id}><div className="review-top"><span className="review-avatar">{review.avatar}</span><div><strong>{review.name}</strong><small>{review.date} · {products[index].name}</small></div><span style={{ marginLeft: "auto" }}><RatingStars rating={review.rating} compact /></span></div><p>{review.content}</p><Button variant="outline" size="sm">Phản hồi</Button></div>)}</section></>; }
